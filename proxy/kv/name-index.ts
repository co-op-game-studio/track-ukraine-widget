/**
 * Name-search normalization + ranking helpers.
 *
 * Preparatory re-export for Phase 12.
 *
 * Traces: FR-42, FR-31.
 */
export {
  normalizeSearchKey,
  rankMatches,
  type NameIndexEntry,
} from '../lib';
